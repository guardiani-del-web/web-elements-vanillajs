'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-869cff5b.js');

const DropdownGroup = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        /** Define the orientation of the objects inside this group, "column" or "row" */
        this.orientation = 'column';
    }
    componentDidLoad() {
        const items = this.el.querySelectorAll(':scope > we-dropdown-item');
        for (let i = 0; i < items.length - 1; i++) {
            if (this.orientation === 'column')
                items[i].setAttribute('border-class', 'border_bottom');
            else
                items[i].setAttribute('border-class', 'border_right');
        }
    }
    render() {
        return (index.h(index.Host, { class: this.orientation }, index.h("slot", null)));
    }
    get el() { return index.getElement(this); }
    static get style() { return ":host{display:var(--display, flex);width:var(--width, -webkit-fit-content);width:var(--width, -moz-fit-content);width:var(--width, fit-content);height:var(--height, auto);background:var(--background, white);border:var(--border, solid 0.125em black)}:host(.row){-ms-flex-direction:var(--flex-direction, row);flex-direction:var(--flex-direction, row)}:host(.column){-ms-flex-direction:var(--flex-direction, column);flex-direction:var(--flex-direction, column)}"; }
};

exports.we_dropdown_group = DropdownGroup;
