import { ComponentInterface } from '../../stencil-public-runtime';
export declare class AccordionGroup implements ComponentInterface {
    /** Identify if this accordion group contains more accordions or only once */
    multiple: boolean;
    el: HTMLWeAccordionGroupElement;
    accordionCallbackHandler(event: CustomEvent): void;
    render(): any;
}
