import { ComponentInterface, EventEmitter } from '../../stencil-public-runtime';
export interface CheckboxGroupValue {
    value: string;
    children: Array<any>;
}
export declare class CheckboxGroup implements ComponentInterface {
    el: HTMLWeCheckboxGroupElement;
    /** Value that identify this checkbox group */
    value: string;
    /** Event triggered when a checkbox inside change its state that returning the value of checkbox group and the value of checkbox changed */
    checkboxGroupCallback: EventEmitter<CheckboxGroupValue>;
    children: any[];
    checkboxCallbackHandler(prop: any): void;
    componentDidLoad(): void;
    render(): any;
}
