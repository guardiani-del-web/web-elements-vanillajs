import { ComponentInterface, EventEmitter } from '../../stencil-public-runtime';
export interface RadioGroupValue {
    value: string;
    children: Array<any>;
}
export declare class RadioGroup implements ComponentInterface {
    el: HTMLWeRadioGroupElement;
    /** Value that identify this radio group */
    value: string;
    /** Event triggered when a radio button inside change its state that returning the name of radio group and the value of radio button checked */
    radioGroupCallback: EventEmitter<RadioGroupValue>;
    children: any[];
    radioCallbackHandler(prop: any): void;
    componentDidLoad(): void;
    render(): any;
}
