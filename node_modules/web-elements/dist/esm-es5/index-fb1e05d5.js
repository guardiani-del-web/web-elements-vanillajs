function parseFunction(fn) {
    if (fn) {
        if (typeof fn === 'string') {
            return new Function("return " + fn)();
        }
        else if (typeof fn === 'function') {
            return fn;
        }
        else {
            return new Function();
        }
    }
}
function generateUniqueId() {
    return '_' + Math.random().toString(36).substr(2, 9);
}
export { generateUniqueId as g, parseFunction as p };
