import { h, Host } from "@stencil/core";
import { generateUniqueId } from '@utils';
export class Accordion {
    constructor() {
        this.openId = generateUniqueId();
        this.closeId = generateUniqueId();
    }
    handleChange(event) {
        this.accordionCallback.emit(event.target.value);
    }
    render() {
        return (h(Host, { "data-id": this.openId },
            h("input", { type: "radio", name: "accordion", id: this.openId, checked: this.open, value: this.openId, onChange: this.handleChange.bind(this) }),
            h("section", { class: "accordion" },
                h("label", { class: "accordion-title", htmlFor: this.openId },
                    h("slot", { name: "title" })),
                h("label", { class: "accordion-close", htmlFor: this.closeId }),
                h("div", { class: "accordion-content" },
                    h("slot", { name: "content" }))),
            h("input", { type: "radio", name: "accordion", id: this.closeId })));
    }
    static get is() { return "we-accordion"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["accordion.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["accordion.css"]
    }; }
    static get properties() { return {
        "open": {
            "type": "boolean",
            "mutable": false,
            "complexType": {
                "original": "boolean",
                "resolved": "boolean",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": "Indentify if accordion is opened of not"
            },
            "attribute": "open",
            "reflect": false
        }
    }; }
    static get states() { return {
        "openId": {},
        "closeId": {}
    }; }
    static get events() { return [{
            "method": "accordionCallback",
            "name": "accordionCallback",
            "bubbles": true,
            "cancelable": true,
            "composed": true,
            "docs": {
                "tags": [],
                "text": "Event triggered each time the accordion is opened/closed"
            },
            "complexType": {
                "original": "any",
                "resolved": "any",
                "references": {}
            }
        }]; }
}
