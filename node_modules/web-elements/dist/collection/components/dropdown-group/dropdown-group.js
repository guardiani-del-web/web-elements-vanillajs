import { Host, h } from "@stencil/core";
export class DropdownGroup {
    constructor() {
        /** Define the orientation of the objects inside this group, "column" or "row" */
        this.orientation = 'column';
    }
    componentDidLoad() {
        const items = this.el.querySelectorAll(':scope > we-dropdown-item');
        for (let i = 0; i < items.length - 1; i++) {
            if (this.orientation === 'column')
                items[i].setAttribute('border-class', 'border_bottom');
            else
                items[i].setAttribute('border-class', 'border_right');
        }
    }
    render() {
        return (h(Host, { class: this.orientation },
            h("slot", null)));
    }
    static get is() { return "we-dropdown-group"; }
    static get encapsulation() { return "shadow"; }
    static get originalStyleUrls() { return {
        "$": ["dropdown-group.scss"]
    }; }
    static get styleUrls() { return {
        "$": ["dropdown-group.css"]
    }; }
    static get properties() { return {
        "orientation": {
            "type": "string",
            "mutable": false,
            "complexType": {
                "original": "string",
                "resolved": "string",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": "Define the orientation of the objects inside this group, \"column\" or \"row\""
            },
            "attribute": "orientation",
            "reflect": false,
            "defaultValue": "'column'"
        }
    }; }
    static get elementRef() { return "el"; }
}
