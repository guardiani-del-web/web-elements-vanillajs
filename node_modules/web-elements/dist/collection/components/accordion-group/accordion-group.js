import { h } from "@stencil/core";
export class AccordionGroup {
    accordionCallbackHandler(event) {
        const value = event.detail;
        if (!this.multiple) {
            const accordions = this.el.querySelectorAll('we-accordion');
            accordions.forEach((accordion) => {
                if (accordion.getAttribute('data-id') === value) {
                    accordion.setAttribute('open', 'true');
                }
                else {
                    accordion.setAttribute('open', 'false');
                }
            });
        }
    }
    render() {
        return h("slot", null);
    }
    static get is() { return "we-accordion-group"; }
    static get encapsulation() { return "shadow"; }
    static get properties() { return {
        "multiple": {
            "type": "boolean",
            "mutable": false,
            "complexType": {
                "original": "boolean",
                "resolved": "boolean",
                "references": {}
            },
            "required": false,
            "optional": false,
            "docs": {
                "tags": [],
                "text": "Identify if this accordion group contains more accordions or only once"
            },
            "attribute": "multiple",
            "reflect": false
        }
    }; }
    static get elementRef() { return "el"; }
    static get listeners() { return [{
            "name": "accordionCallback",
            "method": "accordionCallbackHandler",
            "target": undefined,
            "capture": false,
            "passive": false
        }]; }
}
